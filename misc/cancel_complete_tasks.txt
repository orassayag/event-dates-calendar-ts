/* cSpell:disable */

Completed + cancelled tasks:
============================
-Sync service - Bring archive days and sync it into an existing document. - Cancelled. *
-Convert all the tasks here into English. - Cancelled. *
-Last stopped on: src\services\events.ts - line 62. *
-במשימה של events dates לעשות כמה משימות שאפשר + לוודא שאפשר להפיק מסמך חדש. *
-Change the scan function name to "validate document". - Cancelled. *
-Previous version bugs: - Cancelled. *
-End year tasks not working. - Cancelled. *
-To Fix the weekend and alter weekend - The alt weekend need to add only additional tasks. - Cancelled. *
-Fix event which will not start with (0). - Cancelled. *
-When it right on the specific day the reminder like "-תוקף אחריות מקלדת ועכבר מKSP עד: 23/04/2023" it missing dot in the end. - Cancelled. *
-A lot of places missing dot in the end. - Cancelled. *
-Backup again before continue. *
-Rebuild this project - More simpler - In TypeScript. *
-Finish the basic of the basic - Just generate a new document. *
-Make sure events from the start ot the document are in the days items. *
-Continue with events-calendar on the PC. *
-Try to finish the refactor of the events calendar dates. *
-We need in this function to start print all the events and the tasks together (text + sub text). - Cancelled. *
-Need to add "Hofesh" in case isVication: true. *
-Need to figure out in which order to do it. *
-Need to use "getDaysCountBetweenDates" function to calclulate the days: *
-a. for the tasks - from "src\data\dates.ts", *
-b. for event years like us holidays, use "startYear" fields. *
-Move all the Hebrew and the English words to culture.ts file. *
-Check that it prints all as expected, compare to original. *
-The second phase of this project is to auto-sync from archive days from the Mac. - Cancelled. *
-Convert all birthdays, death dates, and marriage dates into array on the doc, and implement it by each day. *
-For each future event there is an empty break line space in the end. *
-Auto sync future events: *
-1. All event that not equal to the target year. *
-2. The events that don't match, keep them there. *
-3. Write the entire "future events" section with left events. *
-Add the "Events to add to the events calender". *
-Add vacation days on events calendar for this time. - Cancelled. *
-Once a 3 months - Download papers from: Bank, HotMobile, Clalit. *
-להוסיף בEvent Dates כל יום אחרון לחודש להוריד חשבוניות האתר של בנק הפועלים / Hot Mobile / כללית. *
-Add for my birthdate - אני - יום הולדת לאור אסייג (38). *
-Each end of year ask for Google backup into the iOmega. *
-Also remove empty break line space on the first day of the year: - Cancelled. *
-Check that in the first day of the year we scheduled teeth cleaner and dentists. *
-Fix the "No files matching the pattern "'src/**/*.{ts,tsx}'" were found." when running "pnpm lint". *
-משימה לevents calander - להוסיף מספרי נרות בחנוכה. *
-בסופ"ש: להוסיף בEvent Calander שצריך להחליף מברשת שיניים כל 3 חודשים. *
-להוסיף בEvent Dates לבדוק ממתי יום המשפחה. + להשלים ממתי יום המשפחה. - Alreay Done. *
-Add all the events of the calander according to the ones on Vim. *
-Fix calander events expect daily. *
-Make the infra to run several scripts in this project. *
Feat: Summary results: *
-For each script running we want to write the results. *
-For example, for the "create" script, we will see the results like this: *
===CREATE TEXT FILE===: *
===TOTAL EVENTS: 3,443=== *
===FILE SIZE: 154KB | LINES COUNT: 9,330=== *
-After ===EXIT: FINISH=== add ===DIST: event-dates-2023 (102KB)===. *
Feat: Sync Script: *
-The sync script will be run by "sync" ("pnpm run sync"). *
-From the sources folder: *
-If the "event-dates-####.txt" (The #### is from the target year on settings.ts) file not exists in the "sources" folder on the root - Throw an error. *
-If the "event-dates-archive-####.txt" (The #### is from the target year on settings.ts) file not exists in the "sources" folder on the root - Throw an error. *
-What the sync script do:
1. Scan the 2 txt files. *
2. For each day (comparing by the title) in the "event-dates-####.txt" file you need to match the one from "event-dates-archive-####.txt". *
a. For example, if in the "event-dates-####.txt" file we have: "28/10/2025 שלישי.", we will need to find the match for it on "event-dates-archive-####.txt". *
b. The matching should be advanced: should match included "28/10/2025" or "28.10.2025" or "28-10-2025" (not require any other text). *
c. If not match found, write it on the results summary. *
3. After we have the matching days, we need to merge the daily tasks list. *
a. The order: the daily tasks from "event-dates-####.txt" file, then the matching tasks of the specific date from the "event-dates-archive-####.txt". *
4. We need to handle the calculators. *
a. We need to continue from the last count of each event within all the archive days. *
b. For example: in the "event-dates-####.txt" file: "-יום ### לעבודה בVim." all of them should be converted to be within numbers and with * in the end. *
c. "-יום ### לעבודה בVim." will be converted: "-יום 123 לעבודה בVim. *". *
5. Write on the summary all the relevant tasks. *
6. The result summary structure, similar to in the Create.ts script, will be for example: *
================================= *
===SOURCE FILE: EVENT-DATES-2025.TXT=== *
===SIZE: 319KB | DAYS: 365=== *
===LINES: 7,610 | EVENTS: 5,853=== *
================================= *
===ARCHIVE FILE: event-dates-archive-2026.txt *
===SIZE: 319KB | DAYS: 89=== *
===LINES: 7,610 | EVENTS: 5,853=== *
================================= *
===DIST FILE: EVENT-DATES-2026.TXT=== *
===SIZE: 92KB | DAYS: 365=== *
===LINES: 3,602 | EVENTS: 1,873=== *
================================= *
7. The document on the dist should be the same file name and content, but with the merged events and tasks. *
a. For example, it event-dates-2025.txt and event-dates-archive-2025.txt in the sources folder. *
b. Merged and updated file in the dist folder: event-dates-2025.txt. *
In the end of this task - I need to sync both 2025 and 2026 documents and make sure they work. - Cancelled. *
-Do a script which search for duplicate / missing counters. - Re-calculate counters if needed. *
-Do a script which scan for duplicate empty lines. - Fix if exists. *
-In the scan command - Check for duplicate number days count. - Fix if exists. *
-In the fix command - Fix duplicate days count. - Fix if exists. *
-In Scan mode search for more than one break lines. - Fix if exists. *
-It will contains to steps: *
1. To scan for not ending with dot. *
2. Add scan option for more than 1 line break. *
3. Scan for duplicate days counts or missing days counts from special file. *
Feat: Validate Script: *
-From the sources folder: *
-If the "event-dates-####.txt" (The #### is from the target year on settings.ts) file not exists in the "sources" folder on the root - Throw an error. *
-Validate pass year script - Validate all start with "-" and end with ".". - Fix if not. *
-Validate that no multiple spaces. - Fix if exists - Only one space, for example: hello  world -> hello world. *
-Validate that no multiple break lines. - Fix if exists. - To only one break line. *
-Validate that each line ends with ". *". - If not - Fix to end with ". *". *
-Scan the event counters. *
-An event counter is an event in a day with increasing days, or with ###. *
For example: c. "-יום ### לעבודה בVim." and  "-יום 123 לעבודה בVim. *". *
a. Check the last counter of each counters (counter is a daily event which includes ### or a number, if you see a number that is increasing more than 10 times, its a counter). *
b. You need to find the last counted counter. The last day before you see ### on the daily events is the last counter. *
c. If there are duplicate counters of events (For example, "-יום 123 לעבודה בVim. *" duplicate times for example) we need to re-count the counters. *
For example, if we have duplicate: *
"-יום 123 לעבודה בVim. *". *
"-יום 123 לעבודה בVim. *". *
We should fix it on the first occurrence: *
"-יום 123 לעבודה בVim. *". *
"-יום 124 לעבודה בVim. *". *
6. The result summary structure, similar to in the Create.ts script, will be for example: *
================================= *
===SOURCE FILE: EVENT-DATES-2025.TXT=== *
===SIZE: 319KB | DAYS: 365=== *
===LINES: 7,610 | EVENTS: 5,853=== *
================================= *
===DIST FILE: EVENT-DATES-2025.TXT=== *
===SIZE: 321KB | DAYS: 365=== *
===LINES: 7,620 | EVENTS: 5,888=== *
===MISSING START: 42 | MISSING END: 73=== *
===FIXED LINES COUNT: 45=== *
===FIXED LINES=== *
Line 323: -test line example 1. *
Line 437: -בדיקה שורה אחת 1. *
================================= *
-The document on the dist should be the same file name and content, but with the merged events and tasks. *
Feat: Stop Counter: *
-From the sources folder: *
-If the "event-dates-####.txt" (The #### is from the target year on settings.ts) file not exists in the "sources" folder on the root - Throw an error. *
-On the settings.ts we will enter the text to include, foe example: "יום ### למלחמת אוקראינה-רוסיה" (The ### can be a number or can be ###). *
-Also on the settings.ts, we will enter the date from which to remove from the days the specific event counter, the date is include the same day to remove. *
-So, for example, if the settings.ts i declared to stop counter "יום ### למלחמת אוקראינה-רוסיה" from 12/12/2025, it will be removed from the days in the list from this day (the day itself included). *
-If in the settings.ts instead of a date we will have "all" - We wll remove it from all days in the list. *
-The result summary structure, similar to in the Create.ts script, will be for example: *
================================= *
===SOURCE FILE: EVENT-DATES-2025.TXT=== *
===SIZE: 319KB | DAYS: 365=== *
===LINES: 7,610 | EVENTS: 5,853=== *
===EVENTS MATCHED: 74=== *
================================= *
===DIST FILE: EVENT-DATES-2025.TXT=== *
===SIZE: 92KB | DAYS: 365=== *
===LINES: 3,602 | EVENTS: 1,873=== *
===EVENTS REMOVED: 74=== *
================================= *
-The name of the file will be the same in the dist folder (take the year from the file name - If not exist - throw an error). *
Feat: Search Script: *
-On the sources path read all text files. *
-If not files - Throw an error. *
-Add search options in all event dates documents. *
1. Insert the path of the event calendar dates files. *
2. Scan in all files for a search key from settings.ts, for example "move to logic". *
3. It will display all the files and number lines of this, for example: *
-The result summary structure, similar to in the Create.ts script, will be for example: *
================================= *
===SEARCH: "move to logic"=== *
===RESULTS COUNT: 43=== *
===RESULTS=== *
event-dates-2021.txt | Line 324. *
event-dates-2022.txt | Line 325. *
-Validate that each script have "validate" function. *
-Copy the "event-dates-index.txt" from here as the most updated doc. *
-Add the content of the validation service. *
-Do all the "ToDo" tasks around the project. *
-Also need to do all the "ToDo" in the sync function. *
-Do the rest of the "ToDo" issues around the project and in the below section. *
-Convert functions which receive 3 or more params to class. *
-Add description for each function. *
-Scan each throw error and add 1000000. *
-Add the LICENSE + README.md + INSTRUCTIONS.md files. *
-Add new instructions of how to run and what to input step by step in README.md (once done). *
-Need to make sure to explain what scripts we can run and what each script does. *
-Compare all the code in 4 AI's. - Cancelled. *
-Update the description section in the package.json file. *
-Update the file in the examples folder. *
