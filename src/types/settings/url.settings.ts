export type URLSettings = {
  // Determine the URL address to test the internet connection.
  VALIDATION_CONNECTION_URL: string;
  // Determine the URL of which to take the calendar Hebrew events and holidays.
  CALENDAR_IL_URL: string;
  // Determine the URL of which to take the calendar United States events and holidays.
  CALENDAR_US_URL: string;
};
