export enum Status {
  READY = 'ready',
  CONFIRM = 'confirm',
  INITIATE = 'initiate',
  VALIDATE = 'validate',
  START = 'start',
  SCAN = 'scan',
  ABORT = 'abort',
  SUCCESS = 'success',
}
